# Backend ↔ worker contract

## Job payload

All messages are JSON objects:

```json
{
  "command": "sync_all | sync_node | provision_node | remnawave_full_reconcile | remnawave_sync_user",
  "idempotency_key": "uuid-v4",
  "operation_id": "uuid-v4",
  "target_type": "all | node | remnawave | remnawave_user",
  "target_id": "string | null",
  "created_at": "RFC3339 UTC timestamp"
}
```

Rules:

- `idempotency_key` is generated by the backend per command.
- `operation_id` identifies the job instance.
- `target_id` is `null` for `sync_all` and a node id for node-scoped commands.
- `created_at` is UTC ISO-8601.

## RabbitMQ topology

- Exchange: `amnezia.jobs`
- Dead-letter exchange: `amnezia.jobs.dlx`
- Main queues: `amnezia.sync`, `amnezia.provision`
- Retry queues: `amnezia.retry.10s`, `amnezia.retry.1m`, `amnezia.retry.10m`
- Poison queue: `amnezia.poison`

Main and retry queues use quorum queue arguments where supported.

## Internal backend API

The backend exposes worker-only endpoints under `/internal/worker`. Every request must include:

```http
Authorization: Bearer <WORKER_TOKEN>
```

Missing or invalid tokens return `401`.

### Operation state

- `POST /internal/worker/operations/{operation_id}/start`
  - Valid transition: `queued` or `pending` → `running`
  - Response: `{"status":"running","attempts":1}`
- `POST /internal/worker/operations/{operation_id}/succeed`
  - Valid transition: `running` → `succeeded`
  - Body: `{"result":{"optional":"json object"}}`
  - Response: `{"status":"succeeded"}`
- `POST /internal/worker/operations/{operation_id}/fail`
  - Valid transition: `running` → `failed`
  - Body: `{"error":"message","result":{"optional":"json object"}}`
  - Response: `{"status":"failed"}`
- `GET /internal/worker/operations/stale?status=queued&older_than_seconds=30`
  - Returns operations with matching `status` and `updated_at` older than the cutoff.

Stale response shape:

```json
{
  "operations": [
    {
      "id": "operation uuid",
      "kind": "sync_all | sync_node | provision_node | remnawave_full_reconcile | remnawave_sync_user",
      "target_type": "all | node | remnawave | remnawave_user | null",
      "target_id": "node id | null",
      "status": "queued",
      "attempts": 0,
      "updated_at": "RFC3339 timestamp"
    }
  ]
}
```

### Snapshots

- `GET /internal/worker/sync/snapshot`
  - Response: `{"nodes":[<node snapshot>]}`
- `GET /internal/worker/nodes/{node_id}/sync-snapshot`
  - Response: `<node snapshot>`
- `GET /internal/worker/nodes/{node_id}/provision-snapshot`
  - Response: `<node snapshot>`

Node snapshot shape:

```json
{
  "id": "node id",
  "name": "node name",
  "url": "http://agent:8000",
  "token": "node agent token",
  "provision_status": "pending | succeeded | failed",
  "interface": {
    "private_key": "server private key",
    "address": "10.8.0.1/24",
    "listen_port": 51820,
    "jc": 4,
    "jmin": 40,
    "jmax": 70,
    "s1": 0,
    "s2": 0,
    "s3": 0,
    "s4": 0,
    "h1": "1",
    "h2": "2",
    "h3": "3",
    "h4": "4",
    "i1": "",
    "i2": "",
    "i3": "",
    "i4": "",
    "i5": "",
    "mtu": "1376",
    "post_up": "iptables ...",
    "post_down": "iptables ..."
  },
  "peers": [
    {
      "peer_id": "peer id",
      "user_id": "user id",
      "user_name": "alice",
      "public_key": "client public key",
      "allowed_ip": "10.8.0.2",
      "psk_key": "preshared key",
      "status": "pending | active | pending_delete | deleted",
      "is_blocked": false
    }
  ]
}
```

### Results

- `POST /internal/worker/nodes/{node_id}/sync-result`
  - Success body:

```json
{
  "ok": true,
  "interface": {
    "public_key": "server public key",
    "endpoint": "vpn.example.com:51820",
    "listen_port": 51820,
    "mtu": "1376"
  },
  "peers": [
    {
      "public_key": "client public key",
      "status": "active",
      "rx_bytes": 100,
      "tx_bytes": 50,
      "last_handshake": "RFC3339 timestamp"
    }
  ]
}
```

  - Failure body: `{"ok":false,"error":"message"}`
  - Response: `{"status":"ok"}` or `{"status":"failed"}`

- `POST /internal/worker/nodes/{node_id}/provision-result`
  - Success body:

```json
{
  "ok": true,
  "interface": {
    "public_key": "server public key",
    "endpoint": "vpn.example.com:51820",
    "listen_port": 51820,
    "mtu": "1376"
  }
}
```

  - Failure body: `{"ok":false,"error":"message"}`
  - Response: `{"status":"succeeded"}` or `{"status":"failed"}`

## Remnawave Commands

Additional commands for Remnawave synchronization:

- `remnawave_full_reconcile` — fetch all users from Remnawave and sync to local DB
  - `target_type`: `remnawave`
  - `target_id`: `null`
- `remnawave_sync_user` — sync single Remnawave user by UUID
  - `target_type`: `remnawave_user`
  - `target_id`: `<remnawave user uuid>`

## Remnawave Internal API

Endpoints under `/internal/worker/remnawave` (require worker bearer auth):

- `GET /internal/worker/remnawave/config`
  - Response: `{"base_url": "...", "api_token": "...", "enabled": true}`
- `GET /internal/worker/remnawave/polling-state`
  - Response: `{"enabled": true, "due": true}`
- `POST /internal/worker/remnawave/users/upsert`
  - Body: raw JSON list of Remnawave user profiles, not `{ "users": ... }`
  - Response: `{"upserted": ["uuid", ...], "affected_node_ids": ["..."]}`
- `POST /internal/worker/remnawave/users/{uuid}/deleted`
  - Response: `{"status": "delete_requested", "affected_node_ids": ["..."]}`
- `POST /internal/worker/remnawave/reconcile-complete`
  - Body: `{"seen_uuids": ["uuid", ...]}`
  - Response: `{"status": "ok", "purged": 0, "affected_node_ids": ["..."]}`

Remnawave full reconcile is non-destructive. The backend uses `seen_uuids` to find users that were not
present in the latest snapshot, marks those users stale, blocks the local user, and keeps the rows in
the database. Missing remote users are not deleted during reconcile completion. Only users that were
already marked delete-requested are purged later, after their peers are fully removed.

Reconcile completion is idempotent. Repeating the same callback does not re-enqueue already-stale
peers, and the backend returns only the newly affected node ids.

## Webhook

Public endpoint `POST /api/remnawave/webhook` receives Remnawave webhooks:

- Verifies `X-Remnawave-Signature` HMAC SHA-256
- Checks `X-Remnawave-Timestamp` within 300 seconds
- Deduplicates by event key
- Enqueues `remnawave_sync_user` or `remnawave_full_reconcile`
- Returns `{"status": "queued"}` or `{"status": "already_processed"}`

## Polling and failure semantics

- The worker polls `/internal/worker/remnawave/polling-state` to decide whether a full reconcile is due.
- If the Remnawave API returns an HTTP error during reconcile, the worker fails the operation and reports the error to the backend.
- If a single user sync fails, the operation is marked failed and the error is recorded.
- Webhook events are deduplicated by event key (`event_type:timestamp:uuid`). Duplicate events return `{"status": "already_processed"}` without enqueuing a new job.
- Missing or invalid webhook signatures return `401`. Expired timestamps return `400`.
- Ownership is one-way: Remnawave is the source of truth. Amnezia does not push changes back to Remnawave.
- Users created from Remnawave are not automatically linked to existing local users. A new local user is created for each Remnawave profile.
